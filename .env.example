# Copy this file to .env.local for local development and set the same
# values in your Vercel project's Environment Variables for production.
# Get your API key from https://resend.com/api-keys after creating a
# free Resend account.

RESEND_API_KEY=
# The private inbox that receives quote-request leads (never shown on the site):
LEAD_TO_EMAIL=
